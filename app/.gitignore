# Capacitor-generated native projects — regenerated by `npx cap add`.
# Kept out of git for the spike; once a platform is customized (icons,
# entitlements, Info.plist), commit ios/ and android/ deliberately.
/ios/
/android/
/node_modules/

# Capacitor copies site/dist here on `cap sync` — it's the build output, not source.
/www/
